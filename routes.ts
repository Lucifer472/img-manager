/**
 * An Array of routes that is Accessible by Public and Requires no Auth
 * @type {string[]}
 */

export const publicRoutes = ["/", "/verify"];

/**
 * The Prefix is for Api Calls and Should never be protected or Blocked!
 * @type {string}
 */

export const apiPrefix = "/api/";

export const DEFAULT_LOGIN_RED = "/images";
